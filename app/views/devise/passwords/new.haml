-#   Modified 5/4/2012 by Zach Prezkuta
-#   Add a line to advise users that the password-reset
-#   email may go to the Spam folder of their email

.span-12.prepend-6.last
  .floating
    %h2
      = t('.forgot_password')
    =t('.forgot_password_spam')
    - unless devise_error_messages!.empty?
      %i= t('.no_account')

    = form_for(resource, :as => resource_name, :url => password_path(resource_name)) do |f|
      %p
        = f.label :email, t('email')
        = f.text_field :email
      %p
      = f.submit t('.send_password_instructions')
    = render :partial => "devise/shared/links"
